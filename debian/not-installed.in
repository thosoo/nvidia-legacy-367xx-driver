# upstream packaging
.manifest
LICENSE
makeself-help-script.sh
makeself.sh
mkprecompiled
nvidia-installer
nvidia-installer.1
pkg-history.txt

# old-style TLS (before Linux 2.6)
#!armhf#libnvidia-tls.so.#VERSION#
tls_test
tls_test_dso.so

# shipped by src:libglvnd
gl.h
glext.h
glx.h
glxext.h

# installed by dh_installchangelogs
NVIDIA_Changelog

# upstream 367 ICD metadata is not installed directly; packaging generates
# GLVND and non-GLVND variants from debian/nvidia_icd.json.template.
nvidia_icd.json

# built from src:nvidia-modprobe
nvidia-modprobe
nvidia-modprobe.1

# built from src:nvidia-persistenced
nvidia-persistenced
nvidia-persistenced-init.tar.bz2
nvidia-persistenced.1

# built from src:nvidia-settings*
nvidia-settings
nvidia-settings.1
nvidia-settings.desktop
nvidia-settings.png
libnvidia-gtk2.so.#VERSION#
libnvidia-gtk3.so.#VERSION#

# built from src:nvidia-xconfig
nvidia-xconfig
nvidia-xconfig.1

# EGL-Wayland support is supplied by the separately maintained egl-wayland package;
# do not install NVIDIA's bundled 367 copy.
libnvidia-egl-wayland.so.#VERSION#
10_nvidia_wayland.json

# built from src:libglvnd
libGLX.so.0
libGLdispatch.so.0
libOpenGL.so.0

# shipped unversioned by src:nvidia-graphics-drivers
10_nvidia.json
nvidia.icd
libOpenCL.so.1.0.0
nvidia-cuda-mps-server
nvidia-cuda-mps-control
nvidia-cuda-mps-control.1

# not shipped by the 367.134 packaging layout
libGL.la

# not shipped by the 367.134 packaging layout
libnvidia-wfb.so.#VERSION#

# NVIDIA 367.134 does not ship a non-GLVND libEGL.so.#VERSION#;
# libEGL_nvidia.so.#VERSION# is the GLVND vendor implementation.
libEGL.so.#VERSION#
