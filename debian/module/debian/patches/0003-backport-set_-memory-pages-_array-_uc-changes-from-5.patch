Description: include linux/types.h in set_memory/set_pages conftests
 Backport the include needed by newer kernel headers for the
 set_memory_uc(), set_memory_array_uc(), and set_pages_uc() conftests.
Origin: vendor, Debian NVIDIA legacy packaging baseline, from NVIDIA 510.39.01
Forwarded: not-needed
Last-Update: 2026-07-19

--- a/conftest.sh
+++ b/conftest.sh
@@ -366,6 +366,7 @@ compile_test() {
             # Determine if the set_memory_uc() function is present.
             #
             CODE="
+            #include <linux/types.h>
             #if defined(NV_ASM_SET_MEMORY_H_PRESENT)
             #include <asm/set_memory.h>
             #else
@@ -383,6 +384,7 @@ compile_test() {
             # Determine if the set_memory_array_uc() function is present.
             #
             CODE="
+            #include <linux/types.h>
             #if defined(NV_ASM_SET_MEMORY_H_PRESENT)
             #include <asm/set_memory.h>
             #else
@@ -416,6 +418,7 @@ compile_test() {
             # Determine if the set_pages_uc() function is present.
             #
             CODE="
+            #include <linux/types.h>
             #if defined(NV_ASM_SET_MEMORY_H_PRESENT)
             #include <asm/set_memory.h>
             #else
